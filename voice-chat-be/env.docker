# --- 애플리케이션 환경 설정 ---
# ENVIRONMENT: 애플리케이션 실행 환경을 지정합니다. (e.g., development, production, docker)
ENVIRONMENT=docker

# --- 벡터 스토어 (Milvus) 연결 정보 ---
# MILVUS_HOST: docker-compose.yml에 정의된 Milvus Standalone 서비스의 이름
MILVUS_HOST=milvus-standalone
MILVUS_PORT=19530
MILVUS_COLLECTION=voice_conversations

# --- AI 모델 설정 ---
# EMBEDDING_MODEL: 텍스트 임베딩에 사용할 Sentence Transformer 모델의 Hugging Face ID
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# LLM_MODEL: LLM(거대 언어 모델)의 Hugging Face ID
LLM_MODEL=google/gemma-2b-it
# LLM_ENABLED: LLM(거대 언어 모델) 기능 사용 여부 (true/false)
LLM_ENABLED=true
# TTS_ENABLED: TTS(음성 합성) 기능 사용 여부 (true/false)
TTS_ENABLED=true
# HF_HOME: Hugging Face 모델과 데이터셋이 캐시될 컨테이너 내 경로
HF_HOME=/root/.cache/huggingface
# HUGGING_FACE_HUB_TOKEN: Hugging Face Hub 접근 토큰 (Gated 모델 접근에 필요)
HUGGING_FACE_HUB_TOKEN="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"

# --- 로깅 설정 ---
# LOG_LEVEL: 애플리케이션 로그 레벨 (e.g., DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# --- SSL/TLS 설정 ---
# SSL_CRT_FILE: SSL 인증서 파일의 컨테이너 내 절대 경로
SSL_CRT_FILE=/certs/cert.pem
# SSL_KEY_FILE: SSL 개인 키 파일의 컨테이너 내 절대 경로
SSL_KEY_FILE=/certs/key.pem
